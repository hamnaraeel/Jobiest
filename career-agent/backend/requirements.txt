fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
alembic>=1.13,<2.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
pgvector>=0.3,<1.0
email-validator>=2.1,<3.0
python-dotenv>=1.0,<2.0
openai>=1.40,<2.0
pytest>=8.2,<9.0
httpx>=0.27,<1.0
requests>=2.31,<3.0
beautifulsoup4>=4.12,<5.0
pytest-mock>=3.14,<4.0
pypdf>=4.3,<5.0
# Pinned (not a floor like the deps above): newer Playwright releases drop
# browser-binary downloads for older macOS versions. 1.48.0 is the newest
# release confirmed to still ship a macOS 12-compatible Chromium build.
# Bump deliberately, not automatically, and re-verify `playwright install
# chromium` succeeds on your OS before raising this.
playwright==1.48.0
pytest-asyncio>=0.24,<1.0
